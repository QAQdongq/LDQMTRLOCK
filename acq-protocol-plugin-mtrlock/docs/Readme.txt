1、根据传入参数，初始化规约, 同时开启规约处理线程
3、当接收到装置数据时，将会通过recieveNetData函数接收数据，将数据存入接收队列中，待处理线程处理。
4、处理完成后，可根据需要调用sendNetData下发响应到装置，或调用sendData上送到采集网关服务。
5、当接收到采集网关下发的命令时，将会通过recieveData函数接收数据，根据具体命令类型处理后，调用sendNetData下发到装置（RTU）。

